type LYNKNFTEntity @entity {
  id: ID!
  owner: Bytes! # address
  isList: Boolean!
  isStaking: Boolean!
  charisma: Int!
  vitality: Int!
  intellect: Int!
  dexterity: Int!
  name: String!
}

type UserEntity @entity {
  id: ID!
  level: Int! # uint8
}